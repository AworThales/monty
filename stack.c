#include "monty.h"
/**
 * awor_stack - prints a top
 * @head: stack head
 * @reader: line_number
 * Return: no return
*/
void awor_stack(stack_t **head, unsigned int reader)
{
	(void)head;
	(void)reader;
	bus.lifi = 0;
}
